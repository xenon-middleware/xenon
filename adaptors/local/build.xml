<project name="LocalAdaptor" default="build" basedir=".">
	<description>Build file for Local Adaptor</description>

	<property name="octopus.dist" location="../../dist" />

	<property name="build.sysclasspath" value="ignore" />

	<path id="external.jars">
		<fileset dir="${octopus.dist}">
			<include name="*.jar" />
		</fileset>
	</path>

	<target name="build" depends="clean" description="build the JARs and documentation">
		<!-- Create the distribution end classes directory -->
		<mkdir dir="dist" />
		<mkdir dir="build/classes" />

		<!-- Compile the java code from ${src} into ${classes} -->
		<javac srcdir="src" destdir="build/classes" debug="on">
			<classpath refid="external.jars" />
		</javac>

		<jar jarfile="dist/octopus-local-adaptor${version}.jar" basedir="build/classes" includes="**" />

		<jar jarfile="${octopus.dist}/octopus-adaptor-local${version}.jar">
			<manifest>
				<attribute name="Octopus-Adaptors" value="local" />
			</manifest>

			<!-- The name set in the prefix here is used as the name of the adaptor class
		                 e.g. Local -> adaptors.local.LocalAdaptor -->

			<zipfileset dir="dist" prefix="octopus-adaptors/local" />
		</jar>

	</target>

	<target name="clean" description="clean up">
		<delete dir="build" />
		<delete dir="dist" />
	</target>

</project>

<project name="Examples" default="build" basedir=".">
	<description>Build file for the examples</description>

	<property name="xenon.dist" location="../dist" />

	<property name="build.sysclasspath" value="ignore" />

	<path id="default.classpath">
		<fileset dir="${xenon.dist}">
			<include name="*.jar" />
		</fileset>
	</path>

	<target name="build" depends="clean" description="build the JARs and documentation">
		<!-- Create the distribution end classes directory -->
		<mkdir dir="dist" />
		<mkdir dir="build/classes" />

		<!-- Compile the java code from ${src} into ${classes} -->
		<javac srcdir="src" destdir="build/classes" debug="on">
			<classpath refid="default.classpath" />
		</javac>

		<jar jarfile="dist/xenon-examples-${version}.jar" basedir="build/classes" includes="**" />
	</target>

	<target name="clean" description="clean up">
		<delete dir="build" />
		<delete dir="dist" />
	</target>
</project>
